// import pluginFs from 'jest-plugin-fs';

// const Ds = require('../ds');

// const ds = new Ds();
// // eslint-disable-next-line global-require
// jest.mock('fs', () => require('jest-plugin-fs/mock'));

// test('Test ds.write', () => {

// });
